{
  "name": "insurance-sites",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build:audit": "npm run build -w insurance-audit-landing",
    "build:cre": "npm run build -w cre-insurance-landing",
    "dev:audit": "npm run dev -w insurance-audit-landing",
    "dev:cre": "npm run dev -w cre-insurance-landing"
  }
}
